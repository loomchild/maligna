package align.aligner.aligners.moore.translationmodel;

import java.util.LinkedList;
import java.util.List;

import align.splitter.WordSplitter;

public class Trainer {

	public Trainer() {
	}
	
	public TranslationModel train(int iterationCount, 
			List<String> sourceSegmentList, List<String> targetSegmentList) {
		assert sourceSegmentList.size() == targetSegmentList.size();
		assert iterationCount >= 1;
		
		
		return null;
	}
	
	/*
	 * Optymalizacja
	private List< List<String> > splitIntoWords(List<String> sentenceList) {
		WordSplitter splitter = new WordSplitter();
		List< List<String> > list = new LinkedList< List<String> >();
		for (String sentence : sentenceList) {
			List<String> wordList = splitter.split(sentence);
			list.add(wordList);
		}
		return list;
	}
	*/
	
}
